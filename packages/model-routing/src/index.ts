/**
 * Маршрутизация запросов к моделям: чем ходить, каким ключом и что делать после отказа
 * (docs/model-routing.md).
 *
 * Пакет чистый: ни рантайма, ни файлов, ни демона. Потребителей два — `@sovereign/agent-runtime-pi`
 * спрашивает следующую попытку, демон держит здоровье ключей, — и это то, ради чего он пакет, а не
 * папка внутри одного из них (docs/repository-structure.md).
 */

export * from "./failure.ts";
export * from "./key-pool.ts";
export * from "./route.ts";
