/**
 * Маршрут внешнего вида и локали. Отдельно от `plugin-preferences.ts`, хотя файл настроек один: это
 * другое состояние с другим потребителем — оболочка спрашивает его на старте, до всякого знания о
 * плагинах.
 *
 * Форма тела и ответа — `AppearancePreferences`, она же часть `preferences.json`: файл остаётся
 * источником истины, а API — вторым способом его изменить (docs/data-directory.md). Записи плагинов этим маршрутом
 * не отдаются и не меняются: у них свой маршрут и своя форма.
 */

export const preferencesPath = "/api/preferences";
