/**
 * Маршрут конфига демона. Отдельно от `preferences-api.ts`, хотя форма маршрутов одинаковая: это
 * другой файл с другим владельцем — `config.json` пишет человек, `preferences.json` платформа по
 * действию пользователя (docs/data-directory.md).
 *
 * Форма тела и ответа — `Config`, она же содержимое `config.json`: файл остаётся источником истины,
 * а API — вторым способом его изменить. Запись идёт документом целиком, а не по ключу: форма всё
 * равно знает все значения, а `/api/config/:key` завёл бы второе место, где живёт схема конфига
 * (docs/web-api.md).
 */

export const configPath = "/api/config";
