{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["es2023"],
    // Типы Node нужны только тестам пакета: сам SDK ни на что из Node не опирается — тот же код
    // поедет в браузер.
    "types": ["node"]
  },
  "include": ["src"]
}
