pnpm-lock.yaml
dist/

# Ворктри-чекауты агентов внутри репозитория: принадлежат своим веткам, их формат проверяется
# в них самих (git прячет эти каталоги по той же причине).
.worktrees/
.claude/

# Служебный указатель на CLAUDE.md для AI-агентов: одна строка, держим как есть.
AGENTS.md
