/**
 * Неймспейс каталога строк плагина. Демон принимает для вклада `locale-catalog` только `core` или
 * идентификатор плагина (apps/daemon/src/plugins/contribution-registry.ts), поэтому значение — ровно
 * id плагина. Один источник для воркера (объявляет каталог) и браузерной половины (читает его
 * снимком вкладов): дублировать строку нельзя, чтобы копии не разошлись.
 *
 * Строки каталога сюда не импортируются намеренно: каталог приезжает в браузер снимком вкладов, и
 * второй его экземпляр в бандле разошёлся бы с объявленным.
 */

export const messagesNamespace = "starter";
