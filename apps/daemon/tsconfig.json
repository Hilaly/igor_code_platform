{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["es2023"],
    "types": ["node"]
  },
  "include": ["src", "scripts"],
  // Плагины-фикстуры — код плагина, а не платформы: им разрешён нестираемый синтаксис (docs/toolchain.md),
  // и проверять их правилами платформы значит запретить ровно то, что они проверяют.
  "exclude": ["src/plugins/fixtures"]
}
