{
  "name": "@sovereign/daemon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/main.ts .sovereign-dev",
    "seed-runbook": "node src/plugins/seed-runbook-fixtures.ts",
    "start": "node src/main.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test \"src/**/*.test.ts\" \"scripts/**/*.test.ts\"",
    "build": "node scripts/build-artifact.ts"
  },
  "dependencies": {
    "@sovereign/agent-runtime-pi": "workspace:*",
    "@sovereign/model-routing": "workspace:*",
    "@sovereign/protocol": "workspace:*",
    "@sovereign/sdk": "workspace:*",
    "cookie": "^2.0.1",
    "esbuild": "^0.28.1",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
