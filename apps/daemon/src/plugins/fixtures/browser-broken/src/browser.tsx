/**
 * Плагин, чья браузерная часть не собирается: точки входа `./missing-panel.tsx` не существует.
 * Ошибка именно такая, а не синтаксическая, чтобы файл оставался читаемым для форматтера и линтера
 * платформы, — сборку она валит одинаково, а от синтаксической отличается только текстом esbuild.
 */

import { Panel } from "./missing-panel.tsx";

export function View() {
  return <Panel />;
}
