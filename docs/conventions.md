# Соглашения по коду

> Заполняется при выборе стека: конкретные инструменты и их конфигурация.

## Структура каталогов

Где что лежит и по какому принципу.

## Именование

Правила для файлов, модулей, типов, функций, переменных.

## Форматирование и линтинг

Инструмент, конфиг, команда запуска. Форматирование не обсуждается вручную — его делает
инструмент, и это отдельный коммит `chore(format): ...`.

## Тесты

Где лежат, как называются, что покрываем обязательно (новое поведение, каждый исправленный баг).

## Обработка ошибок

Как возвращаем и логируем ошибки, что считаем фатальным.
