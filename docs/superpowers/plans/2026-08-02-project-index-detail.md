# План: индекс проектов и страница проекта

## Задачи

- [x] Добавить маршрут `/projects/:id` и round-trip тесты маршрутизатора.
- [x] Добавить поиск по имени и пути с пустым состоянием результата.
- [x] Сделать активные проекты кликабельными карточками, сохранив независимое меню действий.
- [x] Передавать `projectId` в загрузку сессий и переиспользовать существующий фильтр API.
- [x] Добавить страницу проекта с метаданными, доступностью каталога, числом сессий и переходами.
- [x] Добавить локализацию, стили и тесты поведения индекса.

## Проверка

```text
pnpm --filter web test
pnpm --filter web typecheck
pnpm -r test
pnpm -r typecheck
pnpm eslint .
pnpm prettier --check <изменённые файлы>
```
